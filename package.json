{
  "name": "remarq",
  "version": "1.6.0",
  "private": true,
  "license": "AGPL-3.0",
  "description": "Drop-in annotation and comment layer for any HTML page.",
  "scripts": {
    "start": "node server/index.js",
    "postinstall": "npm install --prefix server",
    "build": "npm install --prefix feedback-layer && node feedback-layer/build.js && cp feedback-layer/dist/feedback-layer.js serve/feedback-layer.js",
    "test:server": "c8 --check-coverage --lines 80 node --test server/test.mjs",
    "test:client": "c8 --check-coverage --lines 80 --include=feedback-layer/src/escape-html.js --include=feedback-layer/src/prompt-builder.js node --test feedback-layer/test/test.mjs",
    "test": "npm run test:server && npm run test:client"
  },
  "devDependencies": {
    "c8": "^10.1.3",
    "puppeteer": "^24.37.3"
  }
}
