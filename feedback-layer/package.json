{
  "name": "feedback-layer",
  "version": "2.0.0",
  "private": true,
  "description": "Lightweight annotation layer for document feedback",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "dependencies": {
    "@apache-annotator/dom": "^0.2.0",
    "@apache-annotator/selector": "^0.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
